import React from 'react';
import { Link, useLocation } from 'react-router-dom';
import map from '../utils/breadcrumbsMap';


export default function Breadcrumbs() {
  // Breadcrumbs removed globally per request
  return null;
}
